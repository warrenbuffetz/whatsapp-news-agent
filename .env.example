# Copy this file to .env.local and fill in your credentials.
# .env.local is gitignored and used by Next.js locally and on Vercel (via dashboard).

# ── Twilio (WhatsApp Sandbox) ────────────────────────────────────────────────
# From https://console.twilio.com
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=

# Sandbox sender, e.g. whatsapp:+14155238886
TWILIO_WHATSAPP_FROM=

# Optional override for local dev tunnels (ngrok). On Vercel, the live request URL
# is derived automatically from proxy headers — update Twilio console instead.
# Example: https://abc123.ngrok-free.dev/api/whatsapp
TWILIO_WEBHOOK_URL=

# ── OpenWeatherMap (free tier) ───────────────────────────────────────────────
# From https://openweathermap.org/api
OPENWEATHER_API_KEY=

# Weather is fetched for Toronto, CA (hardcoded in lib/weather.ts)

# ── Finnhub (free tier) — primary SOXL news + VIX/fundamentals ───────────────
# From https://finnhub.io
FINNHUB_API_KEY=

# ── The News API (optional) — one macro search per brief if set ──────────────
# From https://www.thenewsapi.com
THE_NEWS_API_TOKEN=

# ── Google Gemini ─────────────────────────────────────────────────────────────
# From https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Model ID — used by legacy news brief helpers; SOXL uses gemini-2.5-flash in code
GEMINI_MODEL=gemini-1.5-flash

# ── Telegram: run-club ───────────────────────────────────────────────────────
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ── Telegram: SOXL (dedicated bot + group — required for SOXL cron) ──────────
TELEGRAM_SOXL_BOT_TOKEN=
TELEGRAM_SOXL_CHAT_ID=

# Vercel Cron bearer secret
CRON_SECRET=
